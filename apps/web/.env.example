# Core powers preparation, inventory, planning, commerce, growth and control modules.
VITE_CORE_URL=http://localhost:8080

# Leave the edge unset for a self-contained order/KDS demo.
# Set this to an edge origin (or an origin ending in /api/v1) for durable outlet sync.
VITE_EDGE_URL=http://localhost:8081
