# FeastCloud public contracts

These Apache-2.0 JSON Schemas define the stable interoperability boundary for FeastCloud connectors, packs, devices, plugins, and mutations. Product implementations may be AGPL while third parties can implement these contracts independently.

All schemas use JSON Schema draft 2020-12. A released contract is immutable; compatible additions require a minor schema version and breaking changes require a new major version and migration guide.

